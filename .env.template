# =============================================================================
# GOOGLE CLOUD STORAGE
# =============================================================================

# REQUIRED VARIABLES
GCS_BUCKET_NAME=
GCS_PROJECT_ID=

# OPTIONAL VARIABLES
# Optional: Default folder prefix for uploads
GCS_FOLDER_PREFIX_RAW=data/raw/

# Optional: Default content type for CSV uploads
GCS_DEFAULT_CONTENT_TYPE=text/csv

# BigQuery Configuration
BIGQUERY_DATASET=ezpass_data
BIGQUERY_RAW_TABLE=bronze
BIGQUERY_SILVER_TABLE=silver
BIGQUERY_GOLD_TABLE=gold

# =============================================================================
# AIRFLOW CONFIGURATION
# =============================================================================
AIRFLOW_EMAIL=

# =============================================================================
# MLFLOW CONFIGURATION
# =============================================================================
# MLflow Tracking URI (optional)
# Leave empty to use file-based tracking (local SQLite)
# For production, use a proper MLflow server: http://mlflow-server:5000
# Or use a managed service like Databricks or MLflow on GCP
MLFLOW_TRACKING_URI=

# MLflow experiment name
MLFLOW_EXPERIMENT_NAME=ezpass-fraud-detection

# Local directories for MLflow (used if tracking URI not set)
MLFLOW_TRACKING_DIR=/opt/airflow/mlflow-tracking
MLFLOW_ARTIFACT_DIR=/opt/airflow/mlflow-artifacts

# Enable/disable MLflow tracking

